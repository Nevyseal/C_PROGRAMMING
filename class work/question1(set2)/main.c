#include <stdio.h>
int main(){
    int integer,odd,even;
    printf("\nEnter a number:");
    scanf("%d",&integer);
    if(integer % 2 == 0)
        printf("\n%d is an even number",integer);
    else
        printf("\n%d is odd number.",integer);

    //integer % 2 == 0 :means that if integer divided(%) by 2 the reminder (==) is 0 //








}
