#include<stdio.h>
int main(){
    int width,length,area,perimeter;
    printf("Enter the width:");
    scanf("%d",&width);
    printf("Enter the length:");
    scanf("%d",&length);
    area=length*width;
    perimeter=length+length+width+width;
    printf("The area is equal to:%d",area);
    printf("The perimeter is equal to:%d",perimeter);








}
